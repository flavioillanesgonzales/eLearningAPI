name: Hola Munso

on: [push]

jobs:
  hola:
    runs-on: ubuntu-latest
    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo"
  
  ls:
    runs-on: ubuntu-latest
    steps:
      - name: ls
        run: ls -al
  